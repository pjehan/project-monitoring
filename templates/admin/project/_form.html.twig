{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">
        <i class="fas fa-check"></i>
        {{ button_label|default('Save') }}
    </button>
{{ form_end(form) }}
